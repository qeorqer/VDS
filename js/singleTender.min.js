let addPhone=document.querySelector(".your__offer__auth input[type=tel] + span");addPhone&&addPhone.addEventListener("click",()=>{addPhone.insertAdjacentHTML("beforebegin",'<input type="tel" placeholder="Контактный телефон">')});let priceInputs=document.querySelectorAll(".enter__number input"),priceOutputs=document.querySelectorAll(".row__total__price input"),itemsQuantity=Array.from(document.querySelectorAll(".table__item .quantity")),itemsQuantityToNumber=itemsQuantity.map(e=>parseInt(e.innerHTML));priceInputs&&priceOutputs&&itemsQuantityToNumber&&priceInputs.forEach((e,t)=>{e.addEventListener("change",()=>{priceOutputs[t].value=itemsQuantityToNumber[t]*e.value})});